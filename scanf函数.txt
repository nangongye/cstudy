后面这个题就考察了你是不是真懂scanf的读入方式，
在%c将本来作为终止输入的空格“吃掉了”，
你只能用其它的方式来终止输入，就是用换行（我猜测再来个空格或者制表其实也可以）。
scanf("%d%d",&t,&y);输入值中，要用空格或者换行来终止输入。
%c读一个字符，那就是一个字符，空格也是，换行也是，回车也是，所以它的中断条件很简单。然而int float这类数字，读进去不止一个字符，所以就涉及到终止读入的条件，