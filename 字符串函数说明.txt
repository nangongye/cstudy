strchr函数
函数原型
char *strchr(const char *str, int c)
参数

    str-- 要被检索的 C 字符串。
    c-- 在 str 中要搜索的字符。

功能
在参数str所指向的字符串中搜索第一次出现字符c（一个无符号字符）的位置。

返回值
返回一个指向该字符串中第一次出现的字符的指针(返回从字符串中的这个位置起，一直到字符串结束的所有字符)，如果字符串中不包含该字符则返回NULL空指针
strrchr函数
跟上述一样只不过是从右边开始的